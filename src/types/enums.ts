export enum TechnologyType {
  LANGUAGE = "LANGUAGE",
  BACKEND = "BACKEND",
  FRONTEND = "FRONTEND",
  DATABASE = "DATABASE",
  DEVOPS = "DEVOPS",
  TOOL = "TOOL",
  ORM = "ORM",
  CLOUD = "CLOUD",
  TESTING = "TESTING",
  AUTH = "AUTH",
  API = "API",
  MOBILE = "MOBILE",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  DEACTIVE = "DEACTIVE",
}

export enum UserRole {
  ADMIN = "ADMIN",
  MODERATOR = "MODERATOR",
}
